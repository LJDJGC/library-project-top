The Odin Projectのproject:Library
Assignmentの５から疑似コードを記録していきます。

１、まずHTMLで削除ボタンを作る。
２、それぞれの書籍オブジェクトに一意のidに対するデーだ属性をDOM要素に付与とあるが、私の中ではまだ、どのようにすのかイメージできていない。たしかにid弟子い別したら、消去したらズレがうまれるかもしれないので、対策を考えた￥ナイトだが、まの綿sの中ではその対策法が思い浮かばない。
３，script.jsで削除ボタンを押したら、削除したい書籍をライブラリから削除するコードを書く。addBookToLibと逆のコードを書けば行けるのかな？
bookForm.addEventListener('submit', (e) => {の逆のような処理も必要であると考えられる。


要件：各書籍のディスプレイにボタンを追加して、 readステータスを変更します。
これを容易にするには、本のインスタンスのreadステータスを切り替えるBookプロトタイプ関数を作成する必要があります。

１，削除と同じように追加した、書籍の横にボタンを追加する。
document.createElement('button');で追加し、readステータスのボタン作成。

２，プロトタイプ関数を使用してステータスを読んでいるか読んでいないかにボタン一つで変更するという仕様なのだと思うが、プロトタイプ関数をどのように活用していけばいいのか今の状態ではわからない。